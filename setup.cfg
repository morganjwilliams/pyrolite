[aliases]
test=pytest

[tool:pytest]
addopts = --cov-report term --cov-report html --cov=pyrolite --cov-config .coveragerc

[versioneer]
VCS = git
style = pep440
versionfile_source = pyrolite/_version.py
versionfile_build = pyrolite/_version.py
tag_prefix =
parentdir_prefix = pyrolite-

[metadata]
description-file = README.md
